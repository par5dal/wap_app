<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    
    <!-- 1. Permiso para la ubicación precisa (GPS). Necesario para Geolocator. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    
    <!-- 2. Permiso para la ubicación aproximada (WiFi/Red móvil).
         Es una buena práctica incluirlo como fallback, aunque pidamos la fina. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    
    <!-- 3. Permiso para acceder a internet. Necesario para Dio, Google Maps, etc.
         Flutter lo añade por defecto, pero es bueno asegurarse de que está. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- 4. Cámara: necesario para image_picker (tomar fotos) y permission_handler -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- 5. Notificaciones push: obligatorio a partir de Android 13 (API 33) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 6. Advertising ID: requerido por firebase_analytics en Android 13+ (API 33).
         Google Play exige declarar explícitamente el uso del ID de publicidad. -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID"/>
    
    <application
        android:label="WAP - What a Plan!"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <!-- Pega tu clave de API de Google Maps aquí -->
        <meta-data android:name="com.google.android.geo.API_KEY"
                   android:value="AIzaSyC9YPdFffxK5m6uxTnMKxr71INJfI2lLVg"/>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Deep link: OAuth callback desde Supabase (App Links HTTPS) -->
            <!-- El host se inyecta desde API_BASE_URL en .env vía build.gradle.kts -->
            <!-- Nota: el backend hace HTTP 302 a wap://auth/callback tras el intercambio PKCE.
                 Este filtro es fallback / compatibilidad; el flujo activo usa wap:// -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="${apiCallbackHost}"
                    android:pathPrefix="/auth/callback" />
            </intent-filter>

            <!-- Deep link: eventos (App Links HTTPS) -->
            <!-- Cubre tanto whataplan.net como www.whataplan.net con /{lang}/eventos/{slug} -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="whataplan.net"
                    android:pathPattern="/.*/eventos/.*" />
            </intent-filter>
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="www.whataplan.net"
                    android:pathPattern="/.*/eventos/.*" />
            </intent-filter>

            <!-- Deep link: esquema custom wap:// (fallback / desarrollo) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="wap"
                    android:host="auth"
                    android:pathPrefix="/callback" />
            </intent-filter>
        </activity>
        <!-- Required by image_cropper (UCrop library) -->
        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Queries necesarias para url_launcher -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="tel" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="mailto" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="geo" />
        </intent>
    </queries>
</manifest>
